# ═══════════════════════════════════════════════════════════════
# NEXUS — Environment Variables
# ═══════════════════════════════════════════════════════════════
# Required: at least ONE LLM API key.
# Gemini free-tier (recommended): only GEMINI_API_KEY is needed.
# Get yours free at: https://aistudio.google.com/apikey
# ═══════════════════════════════════════════════════════════════

NEXUS_ENV=development

# ── LLM API Keys (set at least one) ──────────────────────────
# RECOMMENDED — Google Gemini (free tier, sufficient for all agents)
GEMINI_API_KEY=

# OPTIONAL — other LLM providers
GROK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=

# ── Broker (optional — for live/paper trading execution) ─────
ALPACA_API_KEY=
ALPACA_API_SECRET=
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# ── Database (optional — defaults work for local dev) ────────
POSTGRES_USER=nexus
POSTGRES_PASSWORD=nexus_secure_password
POSTGRES_DB=nexus
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DATABASE_URL=postgresql://nexus:nexus_secure_password@localhost:5432/nexus

# ── InfluxDB (optional — time-series storage) ────────────────
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=nexus-influxdb-token
INFLUXDB_ORG=nexus
INFLUXDB_BUCKET=market_data

# ── ChromaDB (optional — vector storage) ─────────────────────
CHROMADB_HOST=localhost
CHROMADB_PORT=8100

# ── Data providers (optional) ────────────────────────────────
NEWSAPI_KEY=
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=nexus-bot/1.0

# ── Observability (optional) ─────────────────────────────────
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

WANDB_API_KEY=
WANDB_PROJECT=nexus

# ── Logging ──────────────────────────────────────────────────
LOG_LEVEL=INFO
LOG_DIR=./logs
